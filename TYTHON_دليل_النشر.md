# بوت تايثون (Tython) — دليل النشر على Railway

## شنو صار بالمشروع؟

هذا المشروع أساسه **WilliamButcherBot** (بايثون + Pyrogram) — بوت حماية مجموعات
متكامل وجاهز فعليًا (٥٥ وحدة/موديول): أدمن، أوامر تحذير (warns)، حظر/طرد،
فلاتر، ملاحظات (notes)، قفل أنواع الرسائل (locks)، ترحيب (greetings)،
قوانين (rules)، بلاك لست، فيدرال بان (feds)، حماية من الفلود، chatbot،
موسيقى، وغيرها الكثير — هذا هو "بوت الحماية المتكامل" اللي تريده، جاهز
من دون ما نبنيه من الصفر.

**ليش ما دمجت bot-help مباشرة؟**
لأن `bot-help` مبني على مكتبة **Telethon** بينما هذا المشروع مبني على
مكتبة **Pyrogram** — المكتبتين ما تشتغلان مع بعض بنفس العملية. فبدل ما
أدمج كود لا يتوافق، أخذت **فكرة** ميزة الاشتراك الاجباري من `bot-help`
وبرمجتها من جديد بأسلوب هذا المشروع (Pyrogram) حتى تشتغل صح وتنسجم
مع باقي الوحدات.

## شنو أضفت أنا؟

1. **`wbb/modules/forcesub.py`** (جديد): اشتراك اجباري *لكل مجموعة على
   حدة* (مو قناة وحدة ثابتة للبوت كله):
   - `/setfsub @channel` أو `/setfsub -100xxxxxxxxxx` — يفعّل الاشتراك
     الاجباري بقناة معينة لهذه المجموعة (للمشرفين فقط).
   - `/unsetfsub` — يعطّل الاشتراك الاجباري لهذه المجموعة.
   - `/fsub` — يعرض حالة الاشتراك الاجباري الحالية.
   - يشترط أن يكون البوت **مشرفًا** داخل القناة حتى يقدر يتحقق من الأعضاء.

2. **`wbb/modules/games_ar.py`** (نسخة من ملفك، وضعتها كما هي داخل مجلد
   الوحدات لأنها مكتوبة أصلاً لتتوافق مع هذا المشروع): اكس أو، صراحة أو
   جرأة، كت، غنيلي، يوت. الوحدة تُحمَّل تلقائيًا — ما تحتاج أي تسجيل يدوي.

3. اسم البوت (**Tython**) ما يحتاج تعديل بالكود إطلاقًا — البوت يقرأ اسمه
   من حساب التليغرام نفسه (اللي تحدده من BotFather)، فقط غيّر الاسم من
   BotFather عادي.

## الأشياء اللي لازم أنت تسويها (ما أقدر أسويها بدالك)

- **إنشاء بوت جديد بـ BotFather** وتسميته **Tython** (أو أي اسم تريده)
  والحصول على `BOT_TOKEN`.
- **الحصول على `API_ID` و `API_HASH`** من my.telegram.org (لازم لأن
  المشروع يشغّل يوزربوت مساعد جنب البوت الأساسي).
- **قاعدة بيانات MongoDB** (يوجد فري تير من MongoDB Atlas) — تحصل منها
  `MONGO_URL`. هذا المشروع يخزن كل الإعدادات (تحذيرات، فلاتر، ترحيب،
  الاشتراك الاجباري...) داخل Mongo — ما يمديك تتجاوزه.
- إنشاء **مجموعة/قناة لوق** واحدة أو أكثر وأخذ الـ ID تبعها لمتغيرات
  `LOG_GROUP_ID` و `GBAN_LOG_GROUP_ID` و `MESSAGE_DUMP_CHAT`.

## رفعه على GitHub

```
cd هذا_المجلد
git init
git add .
git commit -m "Tython - initial merge"
git branch -M main
git remote add origin https://github.com/<حسابك>/Tython.git
git push -u origin main
```

## النشر على Railway

المشروع فيه `Dockerfile` جاهز — Railway يكتشفه تلقائيًا ويبنيه بدون أي
إعداد إضافي. الخطوات:

1. Railway → New Project → Deploy from GitHub repo → اختر مستودع Tython.
2. من تبويب **Variables** ضيف المتغيرات هذي (نفس الأسماء بالضبط):

   | المتغير | الوصف |
   |---|---|
   | `BOT_TOKEN` | توكن البوت من BotFather |
   | `API_ID` | من my.telegram.org |
   | `API_HASH` | من my.telegram.org |
   | `SESSION_STRING` | جلسة اليوزربوت المساعد (شوف `str_gen.py` بالمشروع لتوليدها) |
   | `USERBOT_PREFIX` | بادئة أوامر اليوزربوت، مثلاً `.` |
   | `SUDO_USERS_ID` | آيديات المطورين (أرقام Telegram، مفصولة بمسافة) |
   | `LOG_GROUP_ID` | آيدي مجموعة اللوق |
   | `GBAN_LOG_GROUP_ID` | آيدي مجموعة لوق الحظر العام |
   | `MESSAGE_DUMP_CHAT` | آيدي مجموعة/قناة تخزين مؤقت |
   | `MONGO_URL` | رابط اتصال MongoDB |
   | `WELCOME_DELAY_KICK_SEC` | اختياري، افتراضي 600 |
   | `PM_PERMIT` | اختياري، `True`/`False` |

3. Deploy. راقب اللوقز؛ أول رسالة نجاح تكون
   `BOT STARTED AS Tython!`.

4. بعدين لما تريد تنقله لمكان ثاني (VPS مثلاً)، نفس الـ Dockerfile
   يشتغل — فقط مرّر نفس متغيرات البيئة.

## ملاحظات

- زر "Github" داخل أمر `/repo` وبعض الروابط الداخلية (`/alive`) توجّه
  حاليًا لمستودع WilliamButcherBot الأصلي (المصدر الذي بُني عليه
  المشروع). لما تنشئ مستودعك الخاص باسم Tython، قلي وأحدّث هذي الروابط
  لتشير لمستودعك بدل الأصلي.
- ما قدرت أشغّل أو أختبر البوت فعليًا بهذا الشات (ماكو اتصال إنترنت
  بالبيئة اللي أشتغل فيها)، فقط تأكدت إن الكود صحيح نحويًا (syntax) ومتوافق
  مع بنية المشروع. أول تشغيل حقيقي راح يصير عندك بعد الرفع.
